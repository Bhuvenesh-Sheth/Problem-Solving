import java.util.ArrayDeque;

public class Insertion_in_deque {
    ArrayDeque<Integer> ad = new ArrayDeque<>();

    for(
    int i = 0;i<n;i++)
    {
        ad.offerLast(arr[i]);
    }

    return ad;
}
